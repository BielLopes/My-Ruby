<h1>Blog Post</h1>

<% @posts.each do |post| %>
	<div class="well">
		<h3><%= post.title %></h3>
		<p><%= post.body %></p>
		
		<%= link_to "Laer mais", post_path(post), 
		:class => 'btn btn-primaty' %>
	</div>
<% end %>